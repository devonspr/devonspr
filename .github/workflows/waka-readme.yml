name: ⏲️ Waka Readme

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-readme:
    name: ⏲️ WakaReadme DevMetrics
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master
        with:
          BLOCKS: "⬜🟨🟩"
          COMMIT_MESSAGE: "docs: update waka-readme graph with new metrics"
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
